"use client"
import React from 'react';

const Box = ({ title } : any) => {
    return (
        <button className="bg-slate-200 w-[70px] h-[70px] select-none">
            Theo Park
        </button>
    );
};

export default Box;